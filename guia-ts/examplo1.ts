//aqui iran las propiedades y metodos
/*class example1Pagina{
    private titulo: string;
    private color: string;
    private fuente: string;
    private alineacion: string;

    constructor(titulo: string, color: string, fuente: string){
        this.titulo = titulo;
        this.color = color;
        this.fuente = fuente;
        this.alineacion = "centrado"; //alineacion por defecto
    }

    obtenerInformacion(): void{
        console.log(`titulo: ${this.titulo}`);
        console.log(`color: ${this.color}`);
        console.log(`fuente: ${this.fuente}`);
    }

    setAlineacion(alineacion: string): void{
        if (alineacion === "centrado" || alineacion === "derecha" || alineacion === "izquierda"){
            this.alineacion = alineacion;
        }else {
            console.error("alineacion invalida. opciones validas: 'centrado', 'derecha', 'izquierda'. ");
        }
    }

    imprimirPropiedades(): void{
        console.log(`titulo: ${this.titulo}`);
        console.log(`color: ${this.color}`);
        console.log(`fuente: ${this.fuente}`);
        console.log(`alineacion: ${this.alineacion}`); 
    }
}

//ejemplo de uso
const examplo1 = new cabeceraPagina("mi pagina", "azul", "arial");
example1Pagina.obtenerInformacion();//muestra el titulo, color y fuente
example1Pagina.setAlineacion("derecha");//cambia la alineacion del titulo
example1Pagina.imprimirPropiedades();//muestra todas las propiedades*/
